---
to: "docs/releases/v26.7.6/BREED_ALGORITHM_REGISTRY.md"
force: true
sparql:
  breeds: |
    PREFIX compat: <https://wasm4pm.dev/ns#>
    SELECT ?id ?label ?citation (COALESCE(?standing, "ADMITTED") AS ?status)
    WHERE {
      ?b a compat:CognitionBreed ;
         compat:breedId ?id ;
         compat:breedLabel ?label ;
         compat:citation ?citation .
      OPTIONAL { ?b <http://seanchatmangpt.github.io/praxis/law#standing> ?standing }
    }
    ORDER BY ?id
  algorithms: |
    PREFIX pi: <https://wasm4pm.dev/pi#>
    SELECT ?id ?label ?category ?citation ?standing
    WHERE {
      ?a a pi:ProcessIntelligenceAlgorithm ;
         pi:algorithmId ?id ;
         pi:algorithmLabel ?label ;
         pi:category ?category ;
         pi:citation ?citation ;
         pi:standing ?standing .
    }
    ORDER BY ?id
---
# Breed and Algorithm Registry — v26.7.6

GENERATED by `ggen sync` from `packs/wasm4pm-facts-pack/ontology.ttl`
(facts carried verbatim from `wasm4pm/ggen/ontology/breeds.ttl` and
`algorithms.ttl`). Do not edit by hand: this report is a pure SPARQL
projection of the admitted graph. Breed status is law-derived
(`ontology/rules/breed_standing.n3`), never asserted in the fact files.

## Cognition breeds ({{ breeds | length }})

| breedId | Label | Status | Citation |
|---|---|---|---|
{% for row in breeds -%}
| `{{ row.id }}` | {{ row.label }} | {{ row.status }} | {{ row.citation }} |
{% endfor %}

## Process-intelligence algorithms ({{ algorithms | length }})

| algorithmId | Label | Category | Standing | Citation |
|---|---|---|---|---|
{% for row in algorithms -%}
| `{{ row.id }}` | {{ row.label }} | {{ row.category }} | {{ row.standing }} | {{ row.citation }} |
{% endfor %}
