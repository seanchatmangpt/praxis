---
to: docs/chicago_tdd_tools_boundary.md
sparql:
  tests: |
    PREFIX ctt: <http://seanchatmangpt.github.io/packs/chicago-tdd-tools#>
    SELECT ?test_name ?binary ?args ?exit_code ?axiom
    WHERE {
      ?t a ctt:CliBoundaryTest ;
         ctt:testName ?test_name ;
         ctt:binary ?binary ;
         ctt:args ?args ;
         ctt:expectExitCode ?exit_code ;
         ctt:coversAxiom ?axiom .
    }
    ORDER BY ?test_name
---
# CLI Boundary Proofs (chicago-tdd-tools)

Generated from `ctt:CliBoundaryTest` individuals. Each row is one
Chicago-style `#[test]` in `tests/chicago_tdd_tools_boundary.rs` that
crosses a real binary boundary via `CliHarness` — no mocks.

| Test | Binary | Args | Exit | Axiom covered |
|------|--------|------|------|---------------|
{% for row in tests %}| `{{ row.test_name }}` | `{{ row.binary }}` | `{{ row.args }}` | {{ row.exit_code }} | {{ row.axiom }} |
{% endfor %}
