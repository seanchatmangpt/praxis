[pack]
name = "arazzo-pack"
version = "26.7.10"
description = "Arazzo 1.1.0 workflow YAML and per-engine OpenAPI 3.1 / AsyncAPI 3.0 capability documents projected from the arz: RDF graph. ontology.ttl is this pack's own content (the arzeng: engine capability contract); crates/cng/ontologies/arazzo.ttl (the arz: vocabulary) and crates/cng/examples/arazzo-api-orchestration.ttl (workflow instance data) are unioned via this pack's ggen.toml extra_ontologies entry, not committed here. RDF stays authoritative; the YAML files are projection artifacts whose digests cng verifies downstream (PROJ-726)."
