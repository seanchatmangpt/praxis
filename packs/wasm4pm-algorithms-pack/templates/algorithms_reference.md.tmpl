---
to: docs/w4pm_algorithms.md
sparql:
  algorithms: |
    PREFIX pi: <https://wasm4pm.dev/pi#>
    SELECT ?algorithm_id ?label ?citation ?category ?speed_tier ?wasm_export WHERE {
      ?a a pi:ProcessIntelligenceAlgorithm ;
         pi:algorithmId    ?algorithm_id ;
         pi:algorithmLabel ?label ;
         pi:citation       ?citation ;
         pi:category       ?category ;
         pi:speedTier      ?speed_tier ;
         pi:wasmExport     ?wasm_export .
    } ORDER BY ?algorithm_id
---
# wasm4pm Algorithm Reference

Generated by `wasm4pm-algorithms-pack` from the wasm4pm process-intelligence
algorithm ontology (`pi:ProcessIntelligenceAlgorithm` individuals). Catalog
surface only — all process analysis executes inside wasm4pm.

| Label | Category | Speed tier | WASM export | Citation |
|-------|----------|------------|-------------|----------|
{% for row in algorithms %}| `{{ row.label }}` | {{ row.category }} | {{ row.speed_tier }} | `{{ row.wasm_export }}` | {{ row.citation }} |
{% endfor %}
