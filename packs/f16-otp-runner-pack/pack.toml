[pack]
name = "f16-otp-runner-pack"
version = "26.7.12"
description = "F16 \"Erlang OTP Outer Runner\" (V12-016) refusal-atom and claim-marker vocabulary: the 7 real, triggerable {refused, Atom, _} outcomes apps/arazzo_runner/src/arazzo_runner_broker.erl and arazzo_runner_workflow.erl already raise (source module/site/stage cross-checkable against that Erlang source), the 3 atlas-named L8 claim-ceiling markers (OTP_ARAZZO_RUNNER_ALIVE/WORKFLOW_PROCESS_RESTART_RESUMES/NO_PID_SEMANTIC_IDENTITY), and 3 disclosed HAND_WRITE_REQUIRED gaps re-confirmed absent this session (gen_statem lifecycle, Dispatch Worker Supervisor, Program Registry). Generates crates/multifractal-workflow/src/f16_otp_runner_vocab.rs (pure data tables) consumed by the hand-written crates/multifractal-workflow/src/f16_otp_runner.rs, which defines the actual OTPWorkflowRefused Rust enum and L8 claim-ceiling gate -- this pack supplies vocabulary/documentation data only, never admission-chain, lifecycle, or receipt logic, none of which exists in Rust for this family (it lives, and stays, in apps/arazzo_runner's Erlang)."
