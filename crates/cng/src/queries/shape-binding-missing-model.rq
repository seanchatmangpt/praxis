# shape-binding-missing-model.rq — structural validator query (compiled in via include_str!).
# Placeholders: {PREFIX} = POWL2 vocabulary IRI prefix.
SELECT ?b WHERE { ?b <http://www.w3.org/1999/02/22-rdf-syntax-ns#type> <{PREFIX}ChildBinding> . FILTER NOT EXISTS { ?b <{PREFIX}childModel> ?m } }
