# facts-add.rq — EDB projection (PROJ-704): (action, add-effect node) pairs.
PREFIX pddl: <https://truex.io/ontology/pddl-strips#>

SELECT ?action ?atom
WHERE {
  ?action a pddl:Action ;
          pddl:effect ?atom .
  ?atom a pddl:AddEffect .
}
ORDER BY ?action ?atom
