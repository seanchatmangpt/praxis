//! Generated by ggen from packs/chatman-engine-pack/ontology.ttl. Do not edit.
//!
//! Harness: https://praxis.chatman.io/ontology/ceng#RoutingHarness
//! Fixture schema: crates/praxis-graphlaw/tests/chatman_engine_acceptance/schemas/routing_fixture.schema.json

use praxis_graphlaw::chatman::Refusal;

#[path = "chatman_engine_acceptance/harness/mod.rs"]
mod harness;

#[test]
fn acceptance_accept_hot_path_route_for_bounded_pattern() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/routing/hot_path_route_for_bounded_pattern.json"
    );
    // `run_fixture` takes `&Path`; the fixture path here is a `&'static str`
    // (from `concat!`), which does not coerce to `&Path` at a call site, so
    // it is wrapped explicitly.
    let result = harness::run_fixture(std::path::Path::new(fixture));
    // `run_fixture` returns `Result<harness::AdmittedOutcome, Refusal>`, not
    // `Result<&str, Refusal>`, so the acceptance behavior tag
    // (`hot_path_routed`) is documentation carried by the
    // fixture, not a literal engine return value to pattern-match via
    // `as_deref()`. Only admission itself is asserted here.
    assert!(
        result.is_ok(),
        "acceptance case Accept_HotPathRouteForBoundedPattern must be accepted (expected behavior: \
         `hot_path_routed`), got {result:?}"
    );
}

#[test]
fn acceptance_accept_supported_dialect_route() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/routing/supported_dialect_route.json"
    );
    // `run_fixture` takes `&Path`; the fixture path here is a `&'static str`
    // (from `concat!`), which does not coerce to `&Path` at a call site, so
    // it is wrapped explicitly.
    let result = harness::run_fixture(std::path::Path::new(fixture));
    // `run_fixture` returns `Result<harness::AdmittedOutcome, Refusal>`, not
    // `Result<&str, Refusal>`, so the acceptance behavior tag
    // (`supported_dialect_routed`) is documentation carried by the
    // fixture, not a literal engine return value to pattern-match via
    // `as_deref()`. Only admission itself is asserted here.
    assert!(
        result.is_ok(),
        "acceptance case Accept_SupportedDialectRoute must be accepted (expected behavior: \
         `supported_dialect_routed`), got {result:?}"
    );
}

#[test]
fn falsification_falsify_least_expressive_route_violation() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/routing/least_expressive_route_violation.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::LeastExpressiveRouteViolation(_))),
        "mutation Request the ColdPath route for a pattern the HotPath already admits. must refuse with Refusal::LeastExpressiveRouteViolation, got {result:?}"
    );
}

#[test]
fn falsification_falsify_n3_actuation_refused() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/routing/n3_actuation_refused.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::N3ActuationRefused(_))),
        "mutation Mark the N3 request as an actuation attempt, which no profile may actuate. must refuse with Refusal::N3ActuationRefused, got {result:?}"
    );
}

#[test]
fn falsification_falsify_n3_unavailable_by_profile() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/routing/n3_unavailable_by_profile.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::N3UnavailableByProfile(_))),
        "mutation Route through the N3 dialect while the profile keeps defaultEnabled false. must refuse with Refusal::N3UnavailableByProfile, got {result:?}"
    );
}

#[test]
fn falsification_falsify_route_decision_mismatch() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/routing/route_decision_mismatch.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::RouteDecisionMismatch(_))),
        "mutation Alter the recorded route decision literal so it disagrees with the recomputed route. must refuse with Refusal::RouteDecisionMismatch, got {result:?}"
    );
}

#[test]
fn falsification_falsify_unsupported_dialect() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/routing/unsupported_dialect.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::UnsupportedDialect(_))),
        "mutation Name a dialect outside the closed dialect vocabulary. must refuse with Refusal::UnsupportedDialect, got {result:?}"
    );
}

#[test]
fn falsification_falsify_warm_path_required() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/routing/warm_path_required.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::WarmPathRequired(_))),
        "mutation Raise the pattern constraint count above the HotPath maxConstraints of eight. must refuse with Refusal::WarmPathRequired, got {result:?}"
    );
}

/// Deterministic OCEL evidence seal: replays every fixture of this suite
/// sequentially in sorted order on one thread and seals the suite's
/// `.cargo-cicd/ocel/chatman/<suite>.{ocel,receipt}.json` exactly once —
/// the only OCEL emission path (per-case tests above are assertion-only,
/// because their parallel interleaving would make event order racy).
#[test]
fn zz_ocel_evidence_sealed() {
    harness::seal_suite_evidence(&[
        "fixtures/routing/hot_path_route_for_bounded_pattern.json",
        "fixtures/routing/least_expressive_route_violation.json",
        "fixtures/routing/n3_actuation_refused.json",
        "fixtures/routing/n3_unavailable_by_profile.json",
        "fixtures/routing/route_decision_mismatch.json",
        "fixtures/routing/supported_dialect_route.json",
        "fixtures/routing/unsupported_dialect.json",
        "fixtures/routing/warm_path_required.json",
    ]);
}
