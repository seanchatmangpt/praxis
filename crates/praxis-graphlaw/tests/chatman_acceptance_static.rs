//! Generated by ggen from packs/chatman-engine-pack/ontology.ttl. Do not edit.
//!
//! Harness: https://praxis.chatman.io/ontology/ceng#StaticHarness
//! Fixture schema: crates/praxis-graphlaw/tests/chatman_engine_acceptance/schemas/static_fixture.schema.json

use praxis_graphlaw::chatman::Refusal;

#[path = "chatman_engine_acceptance/harness/mod.rs"]
mod harness;

#[test]
fn acceptance_accept_single_process_receipt_type() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/static/single_process_receipt_type.json"
    );
    // `run_fixture` takes `&Path`; the fixture path here is a `&'static str`
    // (from `concat!`), which does not coerce to `&Path` at a call site, so
    // it is wrapped explicitly.
    let result = harness::run_fixture(std::path::Path::new(fixture));
    // `run_fixture` returns `Result<harness::AdmittedOutcome, Refusal>`, not
    // `Result<&str, Refusal>`, so the acceptance behavior tag
    // (`process_receipt_type_unique`) is documentation carried by the
    // fixture, not a literal engine return value to pattern-match via
    // `as_deref()`. Only admission itself is asserted here.
    assert!(
        result.is_ok(),
        "acceptance case Accept_SingleProcessReceiptType must be accepted (expected behavior: \
         `process_receipt_type_unique`), got {result:?}"
    );
}

#[test]
fn acceptance_accept_unique_canonical_tape_type() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/static/unique_canonical_tape_type.json"
    );
    // `run_fixture` takes `&Path`; the fixture path here is a `&'static str`
    // (from `concat!`), which does not coerce to `&Path` at a call site, so
    // it is wrapped explicitly.
    let result = harness::run_fixture(std::path::Path::new(fixture));
    // `run_fixture` returns `Result<harness::AdmittedOutcome, Refusal>`, not
    // `Result<&str, Refusal>`, so the acceptance behavior tag
    // (`canonical_tape_type_unique`) is documentation carried by the
    // fixture, not a literal engine return value to pattern-match via
    // `as_deref()`. Only admission itself is asserted here.
    assert!(
        result.is_ok(),
        "acceptance case Accept_UniqueCanonicalTapeType must be accepted (expected behavior: \
         `canonical_tape_type_unique`), got {result:?}"
    );
}

#[test]
fn falsification_falsify_duplicate_canonical_tape_type() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/static/duplicate_canonical_tape_type.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::DuplicateCanonicalTapeType(_))),
        "mutation Register the CanonicalTape type identifier twice in the type registry. must refuse with Refusal::DuplicateCanonicalTapeType, got {result:?}"
    );
}

#[test]
fn falsification_falsify_process_receipt_shadow_type() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/static/process_receipt_shadow_type.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::ProcessReceiptShadowType(_))),
        "mutation Declare a second struct named ProcessReceipt shadowing the canonical type. must refuse with Refusal::ProcessReceiptShadowType, got {result:?}"
    );
}

/// Deterministic OCEL evidence seal: replays every fixture of this suite
/// sequentially in sorted order on one thread and seals the suite's
/// `.cargo-cicd/ocel/chatman/<suite>.{ocel,receipt}.json` exactly once —
/// the only OCEL emission path (per-case tests above are assertion-only,
/// because their parallel interleaving would make event order racy).
#[test]
fn zz_ocel_evidence_sealed() {
    harness::seal_suite_evidence(&[
        "fixtures/static/duplicate_canonical_tape_type.json",
        "fixtures/static/process_receipt_shadow_type.json",
        "fixtures/static/single_process_receipt_type.json",
        "fixtures/static/unique_canonical_tape_type.json",
    ]);
}
