//! Generated by ggen from packs/chatman-engine-pack/ontology.ttl. Do not edit.
//!
//! Harness: https://praxis.chatman.io/ontology/ceng#Triple8Harness
//! Fixture schema: crates/praxis-graphlaw/tests/chatman_engine_acceptance/schemas/triple8_fixture.schema.json

use praxis_graphlaw::chatman::Refusal;

#[path = "chatman_engine_acceptance/harness/mod.rs"]
mod harness;

#[test]
fn acceptance_accept_snapshot_contains_only_atomic_terms() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/triple8/snapshot_contains_only_atomic_terms.json"
    );
    // `run_fixture` takes `&Path`; the fixture path here is a `&'static str`
    // (from `concat!`), which does not coerce to `&Path` at a call site, so
    // it is wrapped explicitly.
    let result = harness::run_fixture(std::path::Path::new(fixture));
    // `run_fixture` returns `Result<harness::AdmittedOutcome, Refusal>`, not
    // `Result<&str, Refusal>`, so the acceptance behavior tag
    // (`snapshot_atomic_terms_only`) is documentation carried by the
    // fixture, not a literal engine return value to pattern-match via
    // `as_deref()`. Only admission itself is asserted here.
    assert!(
        result.is_ok(),
        "acceptance case Accept_SnapshotContainsOnlyAtomicTerms must be accepted (expected behavior: \
         `snapshot_atomic_terms_only`), got {result:?}"
    );
}

#[test]
fn acceptance_accept_terms_resolve_in_triple8_universe() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/triple8/terms_resolve_in_triple8_universe.json"
    );
    // `run_fixture` takes `&Path`; the fixture path here is a `&'static str`
    // (from `concat!`), which does not coerce to `&Path` at a call site, so
    // it is wrapped explicitly.
    let result = harness::run_fixture(std::path::Path::new(fixture));
    // `run_fixture` returns `Result<harness::AdmittedOutcome, Refusal>`, not
    // `Result<&str, Refusal>`, so the acceptance behavior tag
    // (`triple8_terms_resolved`) is documentation carried by the
    // fixture, not a literal engine return value to pattern-match via
    // `as_deref()`. Only admission itself is asserted here.
    assert!(
        result.is_ok(),
        "acceptance case Accept_TermsResolveInTriple8Universe must be accepted (expected behavior: \
         `triple8_terms_resolved`), got {result:?}"
    );
}

#[test]
fn acceptance_accept_triple8_universe_within_bounds() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/triple8/triple8_universe_within_bounds.json"
    );
    // `run_fixture` takes `&Path`; the fixture path here is a `&'static str`
    // (from `concat!`), which does not coerce to `&Path` at a call site, so
    // it is wrapped explicitly.
    let result = harness::run_fixture(std::path::Path::new(fixture));
    // `run_fixture` returns `Result<harness::AdmittedOutcome, Refusal>`, not
    // `Result<&str, Refusal>`, so the acceptance behavior tag
    // (`triple8_universe_admitted`) is documentation carried by the
    // fixture, not a literal engine return value to pattern-match via
    // `as_deref()`. Only admission itself is asserted here.
    assert!(
        result.is_ok(),
        "acceptance case Accept_Triple8UniverseWithinBounds must be accepted (expected behavior: \
         `triple8_universe_admitted`), got {result:?}"
    );
}

#[test]
fn falsification_falsify_term_not_in_triple8_universe() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/triple8/term_not_in_triple8_universe.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::TermNotInTriple8Universe(_))),
        "mutation Reference one term IRI that was never interned into the Triple8 universe. must refuse with Refusal::TermNotInTriple8Universe, got {result:?}"
    );
}

#[test]
fn falsification_falsify_triple8_universe_overflow() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/triple8/triple8_universe_overflow.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::Triple8UniverseOverflow(_))),
        "mutation Add a ninth distinct term so the universe exceeds the Triple8 bound of eight. must refuse with Refusal::Triple8UniverseOverflow, got {result:?}"
    );
}

#[test]
fn falsification_falsify_triple8_universe_overflow_by_one() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/triple8/triple8_universe_overflow_by_one.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::Triple8UniverseOverflow(_))),
        "mutation Duplicate one admitted term under a fresh IRI so the universe count becomes nine. must refuse with Refusal::Triple8UniverseOverflow, got {result:?}"
    );
}

#[test]
fn falsification_falsify_triple_term_in_snapshot() {
    let fixture = concat!(
        env!("CARGO_MANIFEST_DIR"),
        "/tests/chatman_engine_acceptance/fixtures/triple8/triple_term_in_snapshot.json"
    );
    let result = harness::run_fixture(std::path::Path::new(fixture));
    assert!(
        matches!(result, Err(Refusal::TripleTermInSnapshot(_))),
        "mutation Embed one RDF-star triple term inside the snapshot term list. must refuse with Refusal::TripleTermInSnapshot, got {result:?}"
    );
}

/// Deterministic OCEL evidence seal: replays every fixture of this suite
/// sequentially in sorted order on one thread and seals the suite's
/// `.cargo-cicd/ocel/chatman/<suite>.{ocel,receipt}.json` exactly once —
/// the only OCEL emission path (per-case tests above are assertion-only,
/// because their parallel interleaving would make event order racy).
#[test]
fn zz_ocel_evidence_sealed() {
    harness::seal_suite_evidence(&[
        "fixtures/triple8/snapshot_contains_only_atomic_terms.json",
        "fixtures/triple8/term_not_in_triple8_universe.json",
        "fixtures/triple8/terms_resolve_in_triple8_universe.json",
        "fixtures/triple8/triple8_universe_overflow.json",
        "fixtures/triple8/triple8_universe_overflow_by_one.json",
        "fixtures/triple8/triple8_universe_within_bounds.json",
        "fixtures/triple8/triple_term_in_snapshot.json",
    ]);
}
