# Claim Promotion Table

Every claim below is verified against a real evidence path Lane 6
personally re-read/re-hashed/re-ran, and against the real, re-derived
`case-study/final_graphlaw_verdict.json` (not asserted independently of it).
Standing column values: `PROMOTED` (evidence-backed, GraphLaw-derived true),
`NOT_READY`, `PARTIAL`.

| # | Claim | Evidence path | GraphLaw derived fact | OCEL event | wasm4pm process evidence | Standing | Scope | Remaining side effects |
|---|---|---|---|---|---|---|---|---|
| 1 | cargo-cicd is the standing emitter | `target/praxis-standing/{standing.json,standing.ttl,standing.ocel.json}` (sha256 in `EVIDENCE_MANIFEST.md`); `case-study/graphlaw_judgment.ttl`'s `praxis:CargoCicd a praxis:Emitter` | `praxis:EvidenceComplete`, `praxis:StandingInputsValid` (both true) | `standing_emitted` x5 | n/a | PROMOTED | local-first fleet standing | none |
| 2 | praxis-graphlaw is the standing judge | `src/bin/case_study_judge.rs`; `case-study/graphlaw_judgment.ttl`'s `praxis:PraxisGraphlaw a praxis:Judge` | `praxis:StandingInputsValid` | `graphlaw_judgment_emitted` | n/a | PROMOTED | this case study | Real bug found+fixed in the judge binary itself (query-projection bug in `verdict_present`; see `GRAPHLAW_JUDGMENT_MODEL.md`). A second, disclosed-but-unfixed graph-consistency artifact of the multi-pass `materialize()` design remains (documented, does not affect the reported verdict) |
| 3 | SHACL validates standing/evidence shapes | `case-study/shapes/*.shacl.ttl` (4 files); `case-study/shacl-report.json`: 4/4 conform, 0 violations | `praxis:ShaclShapesConform` | `shacl_validated` | n/a | PROMOTED | this case study | none |
| 4 | ShEx validates graph structure | `case-study/shex/case-study.shex` (7 shapes); `case-study/shex-report.json`: conforms, 0 failures | `praxis:ShexSchemaConforms` | `shex_validated` | n/a | PROMOTED | this case study | Lane 6 hit and fixed a real ShEx conformance failure while promoting Criteria 10/11/13-15 (predicate misuse — see `GRAPHLAW_JUDGMENT_MODEL.md`) |
| 5 | N3 derives readiness facts | `case-study/rules/judgment.n3` | `praxis:EvidenceComplete`/`ProcessEvidenceValid`/`ClientDisplayValid`/`ExternalSideEffectsSeparated` all true | `n3_materialized` | n/a | PROMOTED | this case study | none |
| 6 | Datalog computes readiness closure | `case-study/rules/readiness.dl.n3` | `unsatisfied_dependency_count: 0`, `praxis:NoTerminalBlockers` | `datalog_closed` | n/a | PROMOTED | this case study | none |
| 7 | PDDL plans lawful validation/repair actions | `case-study/pddl/goal.ttl`, `case-study/pddl-out/plan.json` (16 steps, `powl_chain_hash` identical across 4 independent runs) | Criterion06 satisfied:true | `pddl_plan_generated` | n/a | PROMOTED | this case study | none |
| 8 | POWL models the process | `case-study/powl_model.json` (16 children, 114 order pairs) | Criterion07 satisfied:true | `powl_model_compiled` | conforms per wasm4pm run below | PROMOTED | this case study | none |
| 9 | OCEL records execution | `case-study/ocel_case_study.json` (20 events, 11 objects, sha256 `5260a884...`) | Criterion08 satisfied:true | `ocel_log_written` | integrity_report_summary.valid:true | PROMOTED | this case study | `final_verdict` object remains a declared placeholder (no `final_verdict_rendered` event added — out of Lane 6 scope, see `OCEL_REPLAY_REPORT.md`) |
| 10 | wasm4pm validates the process | `case-study/wasm4pm_validation.json` (`is_conforming: true, fitness: 1.0, violations: []`) | Criterion09 satisfied:true | `wasm4pm_process_validated` | is_conforming:true, fitness:1.0 | PROMOTED | this case study | none |
| 11 | Autonomic Platform displays sourced standing | `clients/autonomic-platform/src/{praxis-adapter.js,praxis-mode.js}`; Playwright 1/1 passed (`case-study-smoke.spec.ts`); screenshot+trace re-generated by Lane 6 showing the real, current, promoted verdict | Criterion12 satisfied:true | `client_smoked` | n/a | PROMOTED | this case study | `clients/autonomic-platform/dist/`, `test-results/` removed after build/test, not committed |
| 12 | no terminal blocker remains for declared local-first scope | `case-study/final_graphlaw_verdict.json`: `unsatisfied_dependency_count: 0`, all 15 criteria satisfied, 0 critical unsatisfied | `praxis:NoTerminalBlockers` | (implied by criteria 6-10's events above) | (implied) | PROMOTED | local-first autonomic release-governance for the seanchatmangpt fleet | 1 known out-of-scope, unrelated pre-existing gap found and FIXED during Lane 6's verification pass (`crates/ggen/tests/dogfood_regression.rs`'s tempdir staging of the `standing-pack` external pack — see the Lane 6 report's verification section); 1 pre-existing, unrelated test failure in the SEPARATE cargo-cicd repo (`ggen_customization_guard::no_forbidden_terms_in_public_docs`), caused by a concurrent session's in-flight docs restructuring there, not fixed (out of scope, live concurrent edit, different repo); `just verify-all`'s `clippy --all-targets --all-features -D warnings` step is RED — 338 pre-existing style-only lints (clone-on-Copy, needless-mut, contains_key+insert, map_or, map().flatten(), derivable impls, etc.) across `crates/praxis-graphlaw`'s legacy/exploratory modules (`dred.rs`, `rsp.rs`, `rsp/s2r.rs`, `csprite.rs`, `backwardchaining.rs`, and others), none in any file touched by Lanes 1-6, none semantic bugs (plain `cargo build`/`cargo test --features ggen` and `cargo check --workspace --all-features` are clean — only the stricter `-D warnings` clippy gate fails); `just check`, `just test`, and `just doctor` are each individually green. Not mass-fixed in this lane: 338 lints across dozens of unrelated files is disproportionate to "the specific failure" this lane's own changes could cause, and an unreviewed batch `cargo clippy --fix` across a crate this size risks regressions beyond Lane 6's scope. Disclosed as a genuine, non-case-study, pre-existing gap for Lane 7 / a follow-up ticket, not silently smoothed over |
| 13 | external operator side effects are separated from release blockers | `docs/standing/EXTERNAL_OPERATOR_SIDE_EFFECTS.md`; every Lane 1-6 report's own "Remaining external side effects" section kept structurally separate from its "Failures found" section; `case-study/graphlaw_judgment.ttl`'s `ExternalSideEffectPlaceholder` (`nonBlocking: true`) | `praxis:ExternalSideEffectsSeparated` | (structural, not a discrete event) | n/a | PROMOTED | this case study + fleet-wide policy | crates.io publish / arXiv submission / repo-visibility change remain real, disclosed, non-blocking operator actions per `EXTERNAL_OPERATOR_SIDE_EFFECTS.md` — unchanged by this case study |
| 14 | the autonomic standing factory is production-ready for declared local-first release-governance scope | `case-study/final_graphlaw_verdict.json`: `verdict: "GRAPHLAW_JUDGED_PRODUCTION_READY_FOR_SCOPE"`, `graph_hash` deterministic across 2 independent Lane 7 runs (`blake3:e82630ce...`, after Criterion06-15 were given real per-criterion `praxis:hasEvidence` links and `criteria[].evidence` was added to the judge's JSON output — verdict unchanged, hash changed because the graph's byte content changed) | `praxis:ProductionReadyForDeclaredScope` | `case_study_finished` | (aggregate of all above) | PROMOTED | local-first autonomic release-governance for the seanchatmangpt fleet (explicitly NOT: public adoption, stable install, GitHub Action, MCP, cross-language, actual external publishes) | See row 12; plus the disclosed graph-consistency artifact in row 2 (does not change the reported verdict, since `case_study_judge`'s priority-ordered verdict selection is itself correct and tested) |

## Claims NOT promoted, or promoted with an explicit caveat

None of the 14 claims above land at `NOT_READY`. All 14 are `PROMOTED`
given the real evidence found across Lanes 1-6. Two explicit caveats carried
through honestly rather than smoothed over:

- Row 9/row 14: the `final_verdict` OCEL object remains a placeholder — Lane
  6 generated the FINAL VERDICT DOCUMENT (`FINAL_VERDICT.md`) but did not
  extend Lane 3/4's OCEL log or POWL process model with a
  `final_verdict_rendered` event, since doing so is outside "evidence
  manifest, claim promotion, and generated reports" and risks an
  unreviewed change to Lane 3/4's process-model contract.
- Row 2/row 14: a real, disclosed (not fixed) graph-consistency artifact
  remains in `case_study_judge`'s multi-pass `materialize()` design (see
  `GRAPHLAW_JUDGMENT_MODEL.md`). The REPORTED verdict is correct and tested
  (the priority-ordered `for v in VERDICTS` loop finds
  `ProductionReadyForDeclaredScope` first, which the fix to
  `verdict_present` now genuinely locates), but a hypothetical direct
  reader of `graphlaw_derived.ttl` would also see a stale
  `NotReadyWithReasons` fact coexisting with it.
