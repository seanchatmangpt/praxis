# Adversarial Review — Praxis v26.7.6 "After Neon"

Method: every credible challenge to this release is mapped to (a) current
standing, (b) evidence (file/test/receipt/commit), (c) the honest boundary of
what the evidence covers, and (d) the next action. Standing values: HOLDS /
HOLDS-WITH-BOUNDARY / OPEN / CONCEDED.

| # | Challenge | Standing | Evidence | Boundary | Next action |
|---|---|---|---|---|---|
| 1 | "The AI hallucinated the math" | HOLDS-WITH-BOUNDARY | Lean 4 kernel is the gauge, not the author's assertion: `crates/praxis-lean` (commit `09dcda2`); 178/202 labels pass `lake env lean` against Mathlib (commit `1ea2385`; `tools/paper-factory/lean-lake/mathlib_migration_receipts.jsonl`) | Kernel acceptance ≠ the statement means what the prose claims; formalization-faithfulness is human-audited, and some files declare local axioms | Produce an axiom census over `tools/paper-factory/lean-lake/Praxis/Corpus/*.lean` and a faithfulness spot-check sample |
| 2 | "A theorem is false" | HOLDS-WITH-BOUNDARY | 4 statements were kernel-rejected and recorded as such rather than hidden (`docs/thesis/math_manufacturing/rdf/thesis.ttl:651`) | Rejection recorded for the standalone lane; the Mathlib lane has 6 unformalized labels whose truth is simply unknown | Keep rejected/unformalized buckets in every published table; never fold them into "verified" |
| 3 | "The graph is wrong" | HOLDS-WITH-BOUNDARY | SHACL/ShEx gates + closed vocabularies refuse malformed or foreign facts by name (`crates/praxis-graphlaw`; `crates/ggen/tests/graphlaw_e2e.rs`; `docs/releases/v26.7.6/GRAPHLAW_FEATURES.md`) | Gates check shape, not truth of admitted facts; shape coverage is unmeasured | Add shape-coverage measurement to the graphlaw e2e suite |
| 4 | "The planner just guessed" | HOLDS-WITH-BOUNDARY | Plans come from PDDL search over graph facts, exercised end to end in `tests/plan_run_e2e.rs` (commit `8336f29`); grounding via `crates/pddl-index` | One demo domain (`examples/v26_7_6_after_neon/goal.ttl`); no optimality claim, only lawfulness of the produced plan | Second domain + a refusal test for unsolvable goals |
| 5 | "The workflow was skipped" | HOLDS-WITH-BOUNDARY | Plan→POWL decomposition is a compiled stage (`crates/powl2-decompose`), and the vertical loop test runs through it (`src/plan_run.rs`, `tests/plan_run_e2e.rs`) | The Lean migration itself was Workflow-driven per commit message `1ea2385` (12 waves), but wave orchestration receipts are in the jsonl, not a POWL receipt chain | Emit POWL-stage receipts in the full-loop demo (RELEASE_CONTROL.md criterion 3) |
| 6 | "bcinr hid semantics in the hot path" | OPEN | bcinr crates are pinned external deps (root `Cargo.toml:96-100`: bcinr-powl-receipt 26.6.24, bcinr-pddl 26.6.26, bcinr-powl 26.6.25) with sources at `/Users/sac/bcinr` (INVENTORY.md) | No praxis-side test currently asserts bcinr's execution semantics against an independent oracle | Add a differential test (praxis `tests/differential.rs` exists as a pattern) over one bcinr hot path |
| 7 | "ggen output is arbitrary" | HOLDS | Determinism tests: `crates/ggen/tests/multi_template_determinism.rs`, `sync_e2e.rs`; lint gates `#![deny(clippy::print_stdout)]`, `#![deny(unsafe_code)]` (`crates/ggen/src/lib.rs`) | Determinism proven per-template, not yet across the full loop twice (criterion 3 NOT STARTED) | Land the two-run byte-identical demo |
| 8 | "The receipt chain is incomplete" | OPEN | Chain test exists (`crates/ggen/tests/receipt_chain_e2e.rs`); receipts on disk (`.ggen/receipts/latest.json`, dated `sync-*.json`) | RELEASE_CONTROL.md criterion 7 ("receipt chain verifies") is UNKNOWN — the verification output for this release has not been captured; RELEASE_CONTROL.md Sec. 7 receipts table is empty | Run chain verification, record output in RELEASE_CONTROL.md Sec. 7/8 |
| 9 | "The report overclaims" | HOLDS-WITH-BOUNDARY | House rule: every claim cites a test/receipt/file (RELEASE_CONTROL.md preamble); the thesis records its own past arithmetic error (218≠219, `rdf/thesis.ttl:63-64`) instead of deleting it; speculative content is fenced (`rdf/thesis.ttl:49`) | Enforcement is editorial, not mechanical | Add a doc-lint pass that flags success adjectives without an adjacent path/test citation |
| 10 | "The TAM is inflated" | CONCEDED (as empirical claim) | The economic estimate is explicitly marked speculative (commit `4de2f55`, "replacement-cost economic estimate (speculative, Sec 5.3)"; thesis Part II fenced as Speculation) | No market claim in this release has empirical standing | Keep all economic figures inside the Speculation fence; none appear in readiness docs as fact |
| 11 | "Fortune-5 outreach is premature" | CONCEDED | FORTUNE5_READINESS.md assigns DEMO_READY overall and ENTERPRISE_HARDENING_REQUIRED to every enterprise-facing surface; no CI, no packaging, no signed receipts | — | Follow the 30/90-day plan; no outreach before pilot entry gate |
| 12 | "The paper is not reproducible" | HOLDS-WITH-BOUNDARY | Per-lane replay exists: `ggen sync` from `rdf/thesis.ttl` regenerates chapters; `lake env lean` replays Lean verdicts; receipts published in-repo | No public artifact bundle; no single-command full-loop replay; single machine | PUBLICATION_PLAN.md steps 3–4 (artifact bundle + determinism demo) before submission |
| 13 | "The release scope is too broad" | HOLDS-WITH-BOUNDARY | RELEASE_CONTROL.md is the single control surface with 7 explicit exit criteria; INVENTORY.md classifies every crate/repo with evidence | 5 of 7 exit criteria UNKNOWN/NOT STARTED at time of writing — scope is declared but not closed | Either close criteria or cut them from v26.7.6 explicitly; no silent carryover |
| 14 | "The code does not build" | HOLDS | `cargo check --workspace` exit 0 (warnings only) on 2026-07-06 (RELEASE_CONTROL.md Sec. 8) | Check ≠ full test suite; `just verify-all` output not captured (criterion 1 UNKNOWN); working tree carries unrelated dirty files | Run and record `just verify-all` |
| 15 | "The tests are partial" | HOLDS-WITH-BOUNDARY | E2e suites exist across the surface: root `tests/` (plan_run_e2e, differential, fuzz_ops, frontier_matrix…), `crates/ggen/tests/` (20+ files incl. combinatorial_matrix, receipt_chain_e2e, graphlaw_e2e) | No coverage measurement; refusal-completeness per command NOT STARTED (criterion 5); `praxis-reconciler` untested (not a workspace member — INVENTORY.md) | Refusal tests per command; adopt or delete praxis-reconciler |
| 16 | "GraphLaw is incomplete" | HOLDS-WITH-BOUNDARY | `crates/praxis-graphlaw` adopted as workspace crate (commit `88216f2`), default GraphEngine in ggen (commit `564543d`), e2e test `graphlaw_e2e.rs` | Adoption is a clean-room rewrite from a 29,625-LOC source (RELEASE_CONTROL.md Sec. 9 scope risk); `graphlaw admit` command listed MISSING in RELEASE_CONTROL.md Sec. 3 at time of writing | Reconcile Sec. 3 command table against the landed commits; add the admit verb or mark it out of scope |
| 17 | "PDDL/POWL integration is fake" | HOLDS | The loop is code, not slideware: `src/plan_run.rs` (322 lines), `src/verbs/plan.rs`, `tests/plan_run_e2e.rs` (119 lines), demo inputs `examples/v26_7_6_after_neon/{goal.ttl,rules.rq,template.md}` — all in commit `8336f29` | One vertical; POWL 2.0 decomposition depth limited to what `powl2-decompose` (Kourani Stage-1) implements | Document decomposition limits in PLANNER_SURFACE.md |
| 18 | "Lean is unavailable / the toolchain won't reproduce" | HOLDS-WITH-BOUNDARY | Mathlib adopted via prebuilt cache with a bounded pilot first (commit `dab70b7`); lake project checked in at `tools/paper-factory/lean-lake/` | Toolchain pinning reproduces on this machine; no CI job proves a cold-start rebuild elsewhere | Add a cold-start rebuild script + record its receipt |
| 19 | "The docs are stale" | HOLDS-WITH-BOUNDARY | INVENTORY.md and RELEASE_CONTROL.md dated 2026-07-05/06 with per-row evidence; thesis chapters are generated from RDF, so doc-drift is a regeneration, not an edit | RELEASE_CONTROL.md Sec. 3/5 already lags the landed graphlaw/planner commits (rows still say MISSING/NOT STARTED); doc count row says 2/15 while more docs exist | Reconcile RELEASE_CONTROL.md status rows against commits `564543d`/`8336f29` and the current doc count |

## Summary

No challenge is answered by assertion; four are OPEN or CONCEDED (6, 8, 10,
11) and must stay visibly open until their next actions produce receipts. The
strongest standing is the verification core (challenges 1, 7, 14, 17); the
weakest is release-closure bookkeeping (8, 13, 19), which is cheap to fix and
should be fixed before any external claim.
