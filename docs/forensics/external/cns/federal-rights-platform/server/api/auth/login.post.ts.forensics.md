# Forensics: federal-rights-platform/server/api/auth/login.post.ts

## Verdict

- classification: FAKE/UNSAFE
- severity: CRITICAL
- recommendation: REPLACE
- v26.7.3 impact: Blocker
- confidence: HIGH

## What is wrong
This backend endpoint performs fake authentication by returning a hardcoded administrator account with a known password.

## Evidence
Lines 320-355:
```typescript
async function findUserByEmail(email: string): Promise<User | null> {
  // This would typically use Prisma or another ORM
  // For demo purposes, returning mock data
  if (email === 'admin@federal.gov') {
    return {
      id: 'user-1',
      email: 'admin@federal.gov',
      username: 'admin',
      firstName: 'Federal',
      lastName: 'Administrator',
      password: await bcrypt.hash('securePassword123!', 10), // In production, this would be stored hashed
      roles: [
        {
          id: 'role-admin',
          name: 'Administrator',
          description: 'Full system administrator',
          permissions: [
            { id: 'perm-1', name: 'admin', resource: 'admin', action: 'admin' as any, conditions: [] }
          ],
          isSystemRole: true,
          createdAt: new Date()
        }
      ],
      permissions: [],
      department: 'IT Security',
      clearanceLevel: 'top_secret' as any,
      isActive: true,
      isMfaEnabled: false,
      lastLogin: new Date(),
      failedLoginAttempts: 0,
      createdAt: new Date(),
      updatedAt: new Date()
    }
  }
  return null
}
```

## Inferred reason
Demo mock bypass left in production code.

## v26.7.3 gate impact
Allows unauthorized administrative access with hardcoded credentials, bypassing database user lookup.

## Required proof to promote
Integrate proper database user query and verify password hash against database record.

## Recommended next action
Replace findUserByEmail with database lookups via ORM.

## Do not do
- Do not check in hardcoded credentials or mock user entities.

## Generated by
Broken/Fake Code Forensics Crawler
Date: 2026-07-03
Repo: cns
Commit: d4f69d010b86a5cf5e27db3e9f3e4938e6ab47bd
