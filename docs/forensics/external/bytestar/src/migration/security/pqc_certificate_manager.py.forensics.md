# Forensics: src/migration/security/pqc_certificate_manager.py

## Verdict

- classification: FAKE
- severity: CRITICAL
- recommendation: REPLACE
- v26.7.3 impact: Blocker
- confidence: HIGH

## What is wrong
CA validate_certificate uses MockDilithium.verify.

## Evidence
Lines 244-245:
```python
            if self.ca_algorithm in [PQCAlgorithm.DILITHIUM_2, PQCAlgorithm.DILITHIUM_3, PQCAlgorithm.DILITHIUM_5]:
                valid = MockDilithium.verify(self.ca_public_key, cert_bytes, certificate.signature)
```

## Inferred reason
CA role relies on mock libraries that always validate any certificate chain.

## v26.7.3 gate impact
Allows untrusted certificates to be accepted as valid, leading to MITM vulnerability.

## Required proof to promote
Valid certificate chain verification via real Dilithium signature validation.

## Recommended next action
Hook up CA validator to a real Dilithium signature validation library.

## Do not do
- Do not allow CA verification functions to call mock signature verifiers.
- Do not use mock verification for X.509 certificate validation.

## Generated by
Broken/Fake Code Forensics Crawler
Date: 2026-07-03
Repo: bytestar
Commit: f5be492df3906ac22a631270b6668374c969f4e5
