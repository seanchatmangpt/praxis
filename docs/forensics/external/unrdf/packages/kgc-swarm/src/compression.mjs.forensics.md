# Forensics: packages/kgc-swarm/src/compression.mjs

## Verdict

- classification: BROKEN
- severity: CRITICAL
- recommendation: FIX
- v26.7.3 impact: Blocker
- confidence: HIGH

## What is wrong
Flawed recursive JSON.stringify replacer.

## Evidence
Line 49:
```javascript
  const normalized = JSON.stringify(data, Object.keys(data).sort());
```

## Inferred reason
Using `Object.keys(data).sort()` as a stringify replacer recursively filters nested keys.

## v26.7.3 gate impact
Deduplication and stable hashing of compressed archives are corrupted.

## Required proof to promote
All nested objects must preserve keys on serialization.

## Recommended next action
Use recursive keys sorting instead of the array-replacer parameter.

## Do not do
- Do not pass a sorted keys array as the replacer parameter of JSON.stringify for deep/nested objects.

## Generated by
Broken/Fake Code Forensics Crawler
Date: 2026-07-03
Repo: unrdf
Commit: f5be492df3906ac22a631270b6668374c969f4e5
