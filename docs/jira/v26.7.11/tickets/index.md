# Milestone Overview: v26.7.11 — Multifractal Workflow

Generated from a 10-agent reconciliation + ticket-drafting workflow, 2026-07-11. Every status
below reflects real, session-verified evidence (commands run, files read, grep results) — see
`../IMPLEMENTATION_STATUS.md` for the full layer-by-layer reconciliation this table was drafted
from. Per `PRD.md:1083` ("No later rail SHALL be used to backfill authority missing from an
earlier rail"), dependency chains below are load-bearing, not decorative: a Rail C/D ticket
genuinely cannot start ahead of its Rail A/B blocker.

**Before picking up PROJ-755, PROJ-757, or PROJ-760, read `../SAFETY_FINDINGS.md` first.**
Several files this milestone's most recent commit added under `apps/` are not merely
mislabeled — they include a live pathway that hot-loads LLM-generated code into a running node,
and a checked-in document asserting fabricated capabilities as completed. That is a separate,
higher-priority remediation than any ticket in this table.

## Ticket index

| # | Name | Rail | Scope | Dependencies | Status |
|---|------|------|-------|---------------|--------|
| 750 | POWL v2 Recursive Workflow Sockets & Parent-Child Closure Geometry | A | **Built and independently double-verified this session.** `SocketPath`/`SocketKind`/`WorkflowSocketId`/`Powl::sockets()`/`Powl::socket_at()`/`ParentChildEdge`/`ParentChildClosure` in `crates/powl2-decompose/src/powl.rs` — additive structures (not new tree-node variants, a deliberate choice to avoid breaking existing exhaustive matches) computed over the existing `Powl` tree via deterministic DFS (`BTreeSet`/`BTreeMap` only). 15 new tests + 4 pre-existing external-cut tests + 7 decompose tests, all passing; clippy/fmt clean; determinism-checked (3x identical runs); confirmed zero downstream breakage in the sole in-workspace dependent (`praxis-graphlaw`). Gap: still zero downstream consumers (not wired into `decompose.rs`/`recompose.rs`/`powl_projection.rs` emission) — structurally sound, not yet used. | — | **ALIVE** (library) |
| 751 | External Cut Admission Wiring & SPARQL Projection (Q) Vocabulary Alignment | A | **Built and independently double-verified this session.** `admit_powl_model()` in `crates/praxis-graphlaw/src/chatman/powl_projection.rs` recursively walks the whole `Powl` tree and calls `validate_external_cut` on every `ExternalCut` node (not just the root) before `powl_to_turtle` emits any RDF — this is now a real gate on the only code path that turns `Powl` into RDF, not an orphaned unit-tested function. `render_model_projection.rq` fully rewritten to the real `powl2:` vocabulary (verified by direct predicate-set comparison against the emitter — full overlap, zero leftover mismatch). Real SPARQL execution added via `run_render_model_projection()`, using oxigraph (already a real workspace dependency, none added) — proven against a real fixture with exact-value assertions (element IRIs, region_id, projection/renderer strings, activity labels, the `precedes` edge), not a stub. 6 new tests + all 4 downstream integration test binaries (14 tests) pass. Gap: `run_render_model_projection` has zero callers outside its own tests — not yet invoked from `ChatmanEngine::admit_transition`. | 750 | **ALIVE** (library) |
| 752 | Tera Renderer (T) & Real Projection Receipt for Receipted Arazzo | A | **Built and independently double-verified this session.** Real `tera` dependency (reused, already pinned elsewhere in the workspace) + `crates/praxis-core/templates/arazzo_projection.tera`, rendering PROJ-751's real `ProjectionRow`s into a genuine Arazzo 1.1.0 JSON document. `ArazzoProjectionReceipt::from_materials()` computes all four digest fields as real BLAKE3 hashes of the real Turtle/query/template/output bytes (independently re-verified by recomputing the hashes outside the constructor and asserting equality — not just "a hash exists"). Round-trip test proves the rendered document parses via `wasm4pm-arazzo`'s own real Arazzo admission (`DocumentIndex::add_document`), not a hand-built fixture. A real bug (duplicate `rdf:type` triples double-counting children) was found and fixed during this build. `air_digest_hex` was honestly left caller-supplied pending PROJ-753 rather than faked — and once PROJ-753 landed later in the same session, the placeholder sentinel was replaced with a real digest. Gap: not yet called from any production runtime path. | 751 | **ALIVE** (library) |
| 753 | Arazzo→AIR Lowering Bridge | B | **Built and independently double-verified this session — the load-bearing gap for all of Rail B is closed.** `crates/wasm4pm-arazzo/src/lower.rs` (new) is the first real function anywhere in the repo lowering a genuinely parsed `ArazzoDescription` into `AirProgram`: step identity, parameter/body expressions, outputs, and a new `AirRouting`/`AirRoutingOutcome` type pair for success/failure routing (End/Retry/GotoStep/GotoWorkflow), including local component-reference dereferencing. Routing content is folded into the canonical BLAKE3 digest and WASM custom section (proven by a test showing the digest changes when routing is stripped, not silently dropped). End-to-end proof at two levels: a hand-written realistic Arazzo document taken through the complete real `parse → resolve → lower → normalize → compile_to_wasm` chain, asserting 7 distinctive source-document strings survive into the compiled WASM bytes; and PROJ-752's own manufactured document extended through the same full chain. Disclosed, not hidden, gaps: parameter `$ref` dereferencing, `RequestBody.replacements`, workflow-level (not step-level) routing, and cross-document component references are explicitly out of scope and refused rather than fabricated. | 752 | **ALIVE** (library) |
| 754 | Typed Refusal Taxonomy for the Arazzo→AIR Compiler | B | **Built and independently double-verified this session.** 3 new `Refusal` variants (`CyclicStepDependency` via iterative DFS over `depends_on`, `UnsupportedCriterion` for non-simple criterion shapes, `MalformedRetryPolicy` for invalid timeout/retry fields), each wired into real previously-unchecked lowering-time constructs with dedicated tests (13 new). "Missing operationId" and "unresolvable reference" were deliberately mapped onto the pre-existing `MissingIdentity`/`UnresolvableReference` variants rather than duplicated — documented as a judgment call, not silently reused. Capstone end-to-end test: a real JSON document with an intentional dangling-dependency defect goes through the complete real pipeline and the specific typed refusal fires, naming both the dangling and offending step ids. | 753 | **ALIVE** (library) |
| 755 | AIR Transition Core: Correct δ_AIR Signature & Remove Direct I/O | C | `apps/air_core/src/air_core.erl`: `transition/2` must return `{S', C}`, not a bare context (verified this session — it never returns a 2-tuple today). Remove `dispatch_http/2`/`dispatch_rdma/3` (direct I/O from inside the transition core, which the PRD explicitly forbids at line 388). Remove the 7 non-functional NIF stub exports (all raise `nif_not_loaded`; see `../SAFETY_FINDINGS.md`). | Rail A/B (750-754) | OPEN |
| 756 | AIR Transition Core: AND/Join Readiness & Deterministic Corpus | C | Implement real multi-predecessor join readiness (today manually faked in the shipped test itself — its own comment admits this). Add `rebar.config`/`.app.src` so `air_core` joins the buildable umbrella (currently silently skipped by `rebar3 compile`). Build a deterministic golden-digest event corpus. | 755 | OPEN |
| 757 | OTP Runner: Workflow Identity, Reaction Events & Restart-from-Replay | D | Add the 10 required identity fields and 9 required reaction-event handlers (none exist — `start_link/1` takes one opaque ID and tracks nothing else). Fix `restart => temporary` (violates "SHALL survive restart"). Fix the dead `air_core:initial_state/0` branch (function doesn't exist — every workflow starts with `undefined` core state). Fix `process_transition/2`'s pattern match against `air_core:transition/2`'s real return shape. **Remove `agentic_heal/4`, `invoke_local_llm/1`, `compile_and_load/1`, `apps/otp_runner/src/otp_runner.erl`, and `atomvm_runner.erl`'s fictional exports — see `../SAFETY_FINDINGS.md`, this is not routine ticket scope.** | 755, 756 | OPEN |
| 758 | Broker Dispatch, Correlation & Return-Admission Path | D | Implement the broker as sole actuation route (pre-actuation verification + post-actuation obligations, PRD §13) and the return-admission chain (PRD §8). Wire `CORRELATION_MISSING`/`RETURN_AUTHORITY_REFUSED`/`DIRECT_ACTUATION_REFUSED`/etc. typed refusals. No broker/correlation code exists anywhere in the repo today. | 755, 757 | PLANNED |
| 759 | Parent-Child Closure & Compensation-as-Workflow | D | Implement closure-law predicates (`all_required`, `any_sufficient`, `quorum(q)`, etc.) as POWL/GraphLaw-owned, not Arazzo-transported. Implement compensation-as-workflow (no generic `rollback()`). Net-new — no existing implementation found. | 757, 758 | PLANNED |
| 760 | [AtomVM Wrapper — Execute AIR Transition Semantics](PROJ-760.md) | E | Replace `atomvm_runner.erl`'s unrelated exports (see `../SAFETY_FINDINGS.md`) with a real wrapper over the AIR transition core; add the missing build entrypoint (no `rebar.config` exists for this app at all). | Rail C (755, 756), Rail D (757) | OPEN |
| 761 | [OTP/AtomVM Differential Conformance Corpus](PROJ-761.md) | E | Shared ordered admitted-event corpus + differential harness (modeled on the existing working pattern at `tests/differential.rs`, which has zero AtomVM coverage today) comparing state digest, result digest, refusal class, and command sequence. | 760 | PLANNED |
| 762 | [Semantic Drift Refusal](PROJ-762.md) | E | Typed `Refusal` fired when the Rail E differential harness detects OTP/AtomVM divergence on any equivalence dimension; negative fixture proving it fires on a seeded divergence. | 761 | PLANNED |
| 763 | OTLP→RDF Admission Mapper (closes gap G11) | F | Map Weaver-admitted OTLP spans into named graph `urn:graph:otel`. Explicitly documented as BLOCKED/not-implemented today at `docs/otel-rdf-handoff.md:14`. | — | OPEN |
| 764 | Rewire OCEL CONSTRUCT queries onto admitted evidence + 5-layer graph separation | F | Re-point existing CONSTRUCT→OCEL projection from the synthetic `obs:` bench-fixture graph onto real admitted `urn:graph:otel`; implement the 5-layer graph separation (G_SOURCE/G_OTEL/G_OCEL/G_RESULT/G_RECEIPT) — currently absent entirely. | 763 | BLOCKED |
| 765 | PROV-O transformation ancestry + CONSTRUCT provenance receipt | F | PROV-O ancestry + digest-chain receipt for the G_OTEL→G_OCEL step. No wiring for this evidence chain exists (existing `prov:` usage in `cng` is a different, unrelated decomp-provenance concern). | 763, 764 | BLOCKED |
| 766 | Multifractal measurement profile & execution measure μ_x | G | Define the measurement-profile schema and per-workflow-family execution measure over declared process scales. Zero existing implementation anywhere — see `../RAIL_G_MEASUREMENT_DESIGN.md` for the concrete instrumentation plan (two independent tracks, real code hooks, no invented metrics). | 764 | BLOCKED |
| 767 | Z(q,ε)/τ(q)/α/f(α) estimator + explicit measurement standing | G | Partition-function/mass-exponent/singularity-spectrum estimator and standing-status enum, including the anti-relabeling guard against calling a family `MULTIFRACTAL_ADMITTED` merely because a curve looks nonlinear. | 766 | BLOCKED |
| 768 | Rail H Toolchain Bridge & Corpus Scaffold | H | **PARTIAL, real progress this session**: (1) confirmed `lean`/`lake` resolve and work via `~/.elan/bin` (`crates/praxis-lean/src/lean.rs` already has this baked in as the default command path — real, not aspirational). (2) Ran `lake build` for real against the pre-existing `tools/paper-factory/lean-lake` corpus (not v26.7.11-authored, predates this milestone) — succeeded, 826 jobs, exit 0. (3) Ran `praxis-lean`'s own `no_sorry` audit against that corpus for the first time — found 71 unauthorized `axiom` declarations under its strict default policy (zero `sorry`/`admit`); some look like standard, legitimate cryptographic assumptions (e.g. collision-resistance) that formal proofs routinely axiomatize, not obvious shortcuts, but that distinction hasn't been made per-axiom yet. Added `crates/praxis-lean/tests/rail_h_existing_corpus_audit.rs` as a regression guard on this count (2/2 passing) so it can't silently grow. Still open: justfile wiring, and this ticket's original "scaffold a NEW Rail H corpus" framing was reconsidered — reusing the existing working Lake package via `Praxis/Corpus/`-style new files (PROJ-769's job) is more disciplined than duplicating package setup. | — | PARTIAL |
| 769 | Rail H: Formalize the 9 Declared Theorem-Bearing Targets | H | Author `.lean` source for the 9 targets at `PRD.md:1035-1043` (substitution closure, bounded descent, no-direct-actuation, parent closure ×2, idempotent duplicate-result, PID-independent identity, OTP/AtomVM equivalence premise, receipt-chain determinism). Zero existing `.lean` content matches any of these 9 targets (exhaustive grep this session). | 768 | PLANNED |
| 770 | Rail H: Negative Fixtures, Manifest, and Verifier Report | H | Negative-fixture corpus (sorry-laden proofs, unauthorized axioms, PID-dependent claims); extend the declaration index into a real RDF↔Lean↔receipt manifest; add a Lean/Lake build-status field to the v26.7.11 Verifier Report. | 768, 769 | PLANNED |

## Wave 2 (771-795): sections not yet covered by any ticket above

PROJ-750..770 map to PRD section 7 (layers) and section 22 (delivery rails). They do not cover
sections 8-12, 15, or the *full* section 18 refusal catalog (only the AIR/Arazzo-scoped subset
is in PROJ-754) — those sections have zero tickets before this wave. Sections 19-20 (acceptance
scenarios, verification ladder) are the actual Definition-of-Done test surface and had no
tickets at all. This wave is real PRD scope, not filler: every row cites the exact section it
implements. Numbering continues from 770; no collision with any PROJ ID used elsewhere in the
repo (highest prior use was PROJ-749, confirmed by repo-wide grep before assigning 750-770).

| # | Name | §PRD | Scope | Dependencies | Status |
|---|------|------|-------|---------------|--------|
| 771 | Process Cell Isolation & No-Shared-State Enforcement | §8 (`PRD.md:460-474`) | Each Chatman Engine instance SHALL be an independent process cell with its own admitted graph view, bounded authority, and local execution; no engine SHALL unlock another's process state via shared memory or agent context. Add a real isolation test: two engine instances, prove neither can observe the other's admitted graph/state except through PROJ-758's broker-mediated return-admission path. | 757, 758 | PLANNED |
| 772 | Closure Law: `all_required` and `quorum(q)` | §9 (`PRD.md:500-521`) | Implement the two closure predicates with explicit formulas given in the PRD (`Close(W) iff forall c in C(W), TerminalAdmitted(c)` and the cardinality-threshold form) as POWL/GraphLaw-owned code, not Arazzo-transported. This is the detailed build-out of PROJ-759's closure-law scope, split out for tractability — PROJ-759 remains the umbrella ticket. | 759 | PLANNED |
| 773 | Closure Law: `any_sufficient`, `ordered_subset`, `policy_decides`, `first_conformant` | §9 (`PRD.md:502-509`) | The remaining four of six initial closure types PRD §9 requires. Same isolation rule as PROJ-772: closure belongs to POWL/GraphLaw, Arazzo transports but never defines authoritative closure. | 772 | PLANNED |
| 774 | Child-Completion-As-Observation Gate | §9 (`PRD.md:525`) | "A child completion signal SHALL be treated as observation until admitted" — a distinct typed state (observed-not-admitted) between a child reporting done and a parent's closure law actually firing; prevents PROJ-772/773's closure predicates from firing on unadmitted signals. | 772, 773 | PLANNED |
| 775 | Compensation Workflow Manufacture Path | §10 (`PRD.md:531-550`) | On remediation of a prior actuation, manufacture a compensation workflow carrying authority, admitted inputs, expected consequence, dispatch, receipt, replay — the detailed build-out of PROJ-759's compensation half. A partial real-world consequence must never be erased from process history (append-only, not overwrite). | 759, 758 | PLANNED |
| 776 | Compensation Catalog & No-Generic-Rollback Guard | §10 (`PRD.md:548-550`) | Typed compensation kinds per the PRD's example list (cancellation, revocation, corrective communication, provisional-state reversal, replacement-document request, exception review, responsible-role notification) — explicitly NOT a generic `rollback()`; add a test that a naive rollback attempt is refused/rejected by construction (no such API exists to call). | 775 | PLANNED |
| 777 | GraphLaw Authority Registry: Executable Dialect Declarations | §11 (`PRD.md:554-588`) | `crates/praxis-core/src/graphlaw_authority.rs` — a real, tested, closed registry of all 14 dialects with `authority` and `quarantine_state` populated from the PRD's own table/section-12 text. The other 6 required categories (input/output classes, receipt requirements, replay surface, executable route) are typed and present as `Option`/empty but honestly left unpopulated — PRD §11 doesn't specify per-dialect values for them, so inventing values would itself be an overclaim; see the module's doc comment. `refusal_codes` populated only for the 3 dialects (N3, POWL v2, Arazzo) that actually have dialect-prefixed codes in §18 — the other 10 dialects having none is a real, recorded gap in the PRD's own refusal catalog, not an omission here. 5/5 tests pass (`CARGO_TARGET_DIR=target/agent-authority-registry just praxis-core-test graphlaw_authority`, this session), full praxis-core suite unaffected (21/21 pass). | — | **ALIVE** |
| 778 | Authority Registry: No-Syntax-Equivalence-Escalation Guard | §11 (`PRD.md:588`) | Enforced by construction, not just tested: `authority_for()` is keyed only by declared dialect name and never inspects payload content, so there is no code path that could escalate authority from embedded syntax. Test `embedded_construct_syntax_inside_n3_does_not_escalate_authority` proves a payload labeled N3 containing literal `CONSTRUCT { ... }` text still resolves to N3's own quarantined authority, not CONSTRUCT's manufacture authority. Passing this session. | 777 | **ALIVE** |
| 779 | N3 Quarantine: Default-Off + Explicit Profile Capability Gate | §12 (`PRD.md:592-604`) | N3 SHALL remain unavailable by default; execution requires explicit profile capability, declared cost bounds, builtin whitelist, controlled execution surface, receipt/replay support. Builds on the router/`ProfileGates` mechanism already found ALIVE this session (`crates/praxis-graphlaw/src/chatman/router.rs` — N3 bit already defaults to disabled there for the audited path; this ticket is about making the *cost-bound* and *builtin-whitelist* halves real, which the existing router doesn't yet do). | 777 | PLANNED |
| 780 | N3 Quarantine: Zero-Direct-Actuation Refusal | §12 (`PRD.md:604-608`) | "An N3 rule that requests direct actuation SHALL be refused" — typed refusal + negative fixture. Default routing SHALL prefer SHACL/ShEx, Oxigraph SPARQL, SPARQL CONSTRUCT, Datalog, bounded closure over N3. | 779 | PLANNED |
| 781 | Receipt Chain: Minimum Event Receipt Fields | §15 (`PRD.md:704-716`) | Every workflow execution event needs: workflow semantic ID, parent semantic ID, event type, event digest, prior receipt head, resulting state digest, command digest, runtime profile, timestamp/logical clock, replay ID — a real struct enforced at every event-emission site, not just documented. Depends on there being a real event stream to attach receipts to (Rail B/C). | 753, 756 | BLOCKED |
| 782 | Replay Verifier: 5-Step Algorithm + Mismatch-Is-Refusal | §15 (`PRD.md:717-726`) | Implement replay exactly as specified: resolve AIR artifact by digest → restore admitted initial state → apply admitted ordered event corpus → recompute state/command digests → verify receipt-head equivalence. "Replay mismatch SHALL be a typed refusal or build failure; it SHALL NOT be logged and ignored" — this last clause is the one most often silently violated in practice; add a test that a deliberately corrupted receipt chain actually fails loud, not just logs a warning. | 781 | BLOCKED |
| 783 | Typed Refusal Catalog: Admission & Authority (9 codes) | §18 (`PRD.md:845-858`) | `RAW_OBSERVATION_NOT_ADMITTED`, `POWL_REGION_NOT_ADMITTED`, `EXTERNAL_CUT_UNDECLARED`, `EXTERNAL_CUT_TYPE_MISMATCH`, `EXTERNAL_CUT_AUTHORITY_MISMATCH`, `BOUNDED_DESCENT_NOT_PROVEN`, `ARAZZO_UNMANUFACTURED`, `ARAZZO_SOURCE_RECEIPT_MISSING`, `ARAZZO_PROJECTION_DIGEST_MISMATCH`, `AMBIENT_AUTHORITY_REFUSED` — each needs a real variant + ≥1 end-to-end negative test. Only a fraction of these exist anywhere today (`UnresolvableReference` in wasm4pm-arazzo is the closest analog to `AIR_REFERENCE_UNRESOLVED`, not this group). | 750, 751 | PLANNED |
| 784 | Typed Refusal Catalog: AIR (4 codes) | §18 (`PRD.md:854-857`) | `AIR_PARSE_REFUSED`, `AIR_REFERENCE_UNRESOLVED` (this one may already partially map to `Refusal::UnresolvableReference`, fixed this session — verify and rename/alias rather than duplicate), `AIR_EXPRESSION_UNSUPPORTED`, `AIR_CRITERION_UNSUPPORTED`. | 753, 754 | PLANNED |
| 785 | Typed Refusal Catalog: Return/Correlation/Broker (8 codes) | §18 (`PRD.md:859-866`) | `DIRECT_ACTUATION_REFUSED`, `BROKER_RECEIPT_PRECONDITION_MISSING`, `CORRELATION_MISSING`, `CORRELATION_MISMATCH`, `RETURN_PROVENANCE_MISSING`, `RETURN_AUTHORITY_REFUSED`, `RETURN_STRUCTURE_REFUSED`, `RETURN_SEMANTIC_REFUSED` — these are already named in PROJ-758's scope description; this ticket is the one that actually forces each to exist as a distinct tested variant rather than an aspiration inside a bigger ticket. | 758 | PLANNED |
| 786 | Typed Refusal Catalog: Closure/Compensation/Replay/Equivalence (5 codes) | §18 (`PRD.md:867-871`) | `CHILD_COMPLETION_UNADMITTED`, `PARENT_CLOSURE_UNSATISFIED`, `COMPENSATION_REQUIRED`, `REPLAY_DIGEST_MISMATCH`, `OTP_ATOMVM_SEMANTIC_DRIFT` (this last one is PROJ-762's "Semantic Drift Refusal" — cross-reference, don't duplicate). | 774, 775, 782, 762 | PLANNED |
| 787 | Typed Refusal Catalog: N3/Measurement (7 codes) | §18 (`PRD.md:872-878`) | `N3_CAPABILITY_MISSING`, `N3_COST_BOUND_EXCEEDED`, `N3_BUILTIN_REFUSED`, `N3_DIRECT_ACTUATION_REFUSED` (cross-ref PROJ-780), `MEASUREMENT_PROFILE_MISSING`, `MEASUREMENT_EVIDENCE_INSUFFICIENT`, `MULTIFRACTAL_CLASSIFICATION_UNADMITTED` (this last one is the anti-relabeling guard already scoped into PROJ-767 — cross-reference). | 780, 766, 767 | PLANNED |
| 788 | Acceptance Scenario Tests: Local Execution & Manufacture (§19.1-19.3) | §19 (`PRD.md:886-896`) | Three scenario tests as literal executable assertions, not prose: local POWL region with no external cut stays local and never manufactures Arazzo; a valid external cut produces a real projected+rendered+receipted Arazzo artifact; handwritten (unmanufactured) production Arazzo is refused with `ARAZZO_UNMANUFACTURED`. | 751, 752, 783 | BLOCKED |
| 789 | Acceptance Scenario Tests: Closure & Duplicate Handling (§19.4-19.7) | §19 (`PRD.md:898-913`) | Remote child closure (one admitted + one unadmitted leaves `all_required` parent open); quorum(2)-of-3 closes despite one child remaining open; OTP crash-and-replay reproduces state digest without changing semantic identity; duplicate correlated result doesn't double-advance the workflow. | 772, 773, 756, 758 | BLOCKED |
| 790 | Acceptance Scenario Tests: Refusal & Compensation (§19.8-19.9) | §19 (`PRD.md:914-920`) | A correlated remote result that fails SHACL/authority admission doesn't unlock dependent steps and enters the declared refused/blocked/compensation path; a partial real-world consequence plus a failure requiring remediation manufactures and dispatches a compensation workflow through the same broker/receipt law. | 758, 775 | BLOCKED |
| 791 | Acceptance Scenario Tests: Equivalence & Evidence (§19.10-19.12) | §19 (`PRD.md:922-932`) | Same AIR artifact + same ordered admitted event corpus produces identical state/result digest, refusal class, and command sequence on OTP vs. AtomVM; replay of admitted OTLP-derived RDF regenerates graph-equivalent OCEL with the expected transformation receipt; insufficient scale/evidence returns `MEASUREMENT_EVIDENCE_INSUFFICIENT`/`PARTIAL_ALIVE`, never a false `MULTIFRACTAL_ADMITTED`. | 761, 765, 767 | BLOCKED |
| 792 | Verification Ladder: Chaos Test Suite | §20 Chaos (`PRD.md:965-980`) | Inject all 10 named failure modes (OTP process death, remote engine restart, duplicate delivery, event reordering, delayed acknowledgment, timeout, partition, stale result, malformed result, receipt corruption) against the real runtime once Rail C/D exist. Hard invariant to test: no chaos case may create unreceipted actuation or false parent closure. | 756, 757, 758 | BLOCKED |
| 793 | Verification Ladder: Stress Profile & Declared Limits | §20 Stress (`PRD.md:982-993`) | Measure concurrent workflow instances, dispatch fan-out, child depth, receipt-chain length, event corpus replay size, OCEL object/event volume — with limits declared by profile, not inferred from "it worked once locally." | 792 | BLOCKED |
| 794 | Verification Ladder: Benchmark Suite (9 separately-reported benchmarks) | §20 Benchmark (`PRD.md:995-1009`) | BCINR local transition latency, wasm4pm Arazzo-to-AIR compile cost, Erlang AIR transition cost, OTP supervision/recovery cost, AtomVM transition cost, broker dispatch overhead, replay throughput, RDF-to-OCEL construction cost, multifractal measurement cost — reported separately per the PRD's explicit "no aggregate benchmark SHALL hide scale changes" rule. | 756, 760, 758, 767 | BLOCKED |
| 795 | Verification Ladder: Verifier Report Generator | §20 Verifier Report (`PRD.md:1011-1027`) | A real report generator (not a hand-written doc) covering all 13 required fields: declared/manufactured/admitted artifacts, refused fixtures, orphan counts, projection digest consistency, AIR conformance corpus result, OTP/AtomVM differential result, broker bypass search result, replay equivalence result, OCEL transformation equivalence result, measurement rail status, Lean/Lake build status. This is the actual Definition-of-Done instrument for the whole milestone — everything else in this table feeds it. | 791, 792, 793, 794, 770 | BLOCKED |
| 796 | Wire the Composed Rail A/B Pipeline into `ChatmanEngine::admit_transition` | A/B (`PRD.md:294-370`) | **The single remaining gap after this session's PROJ-750-754 build.** Every stage (POWL admission → SPARQL projection → Tera render → Arazzo manufacture → AIR lowering → WASM compile) is now real, tested, and independently verified — but the full composition exists only inside `#[cfg(test)]` code (`crates/praxis-core/src/arazzo.rs`'s round-trip test). `ChatmanEngine::admit_transition` (`crates/praxis-graphlaw/src/chatman/engine.rs:566`) calls none of PROJ-751/752/753's functions — confirmed by direct grep of its body, not inference. This ticket adds a real production call path (a new engine stage, or an explicit opt-in entry point) that a caller can actually reach, as opposed to a library that only proves itself in tests. Until this lands, Rail A/B cannot be called `ALIVE` end-to-end per the PRD's own admit_transition-centric model, only "ALIVE as a library." | 750, 751, 752, 753, 754 | OPEN |

## Rail summary

- **Rail A (Projection, 750-752)**: **UPDATED** — no longer scaffolding. POWL socket/closure
  geometry, real admission-gated `validate_external_cut` wiring, vocabulary-corrected and
  actually-executing SPARQL projection (real oxigraph, real result rows), and Tera rendering
  with a receipt hashing real material digests are all built and independently double-verified.
  Every stage is ALIVE as a tested library function. The one remaining gap:
  `ChatmanEngine::admit_transition` calls none of it — every stage is only ever composed inside
  test code, never from a production entry point. See PROJ-796.
- **Rail B (Compilation, 753-754)**: **UPDATED** — the load-bearing gap (Arazzo→AIR lowering)
  is closed. `lower.rs` genuinely lowers a real parsed `ArazzoDescription` into `AirProgram`;
  proven end-to-end (parse→resolve→lower→normalize→compile_to_wasm) with real source content
  surviving into compiled WASM bytes, not just "compiles." Typed refusal coverage for cyclic
  dependencies, unsupported criteria, and malformed retry policy added. Same gap as Rail A:
  real and tested, not yet called from any production path. `wasm4pm-arazzo`/`powl2-decompose`
  remain dev-only dependencies of `praxis-core` — the composition exists only in test code.
- **Rail C (Pure Semantics, 755-756)**: real, compiling Erlang that actually runs — but it
  violates the PRD's own return-shape contract, fakes join-dependency readiness in its own
  test, and performs direct I/O the PRD explicitly forbids in this layer.
- **Rail D (Runtime, 757-759)**: a structurally real supervisor wrapping a runner with a
  confirmed dead-code integration bug (workflows start with undefined core state), a restart
  policy that contradicts the PRD, and code that must be removed, not extended — see
  `../SAFETY_FINDINGS.md`.
- **Rail E (Equivalence, 760-762)**: the module named for this rail contains no AIR semantics
  at all; the "proof of equivalence" is prose contradicted by the actual (non-`gen_statem`)
  implementation and backed by a test that always passes regardless of behavior.
- **Rail F (Evidence, 763-765)**: explicitly documented as not-yet-built in its own handoff doc.
- **Rail G (Measurement, 766-767)**: zero implementation; see the dedicated design doc for the
  concrete build plan.
- **Rail H (Formal Standing, 768-770)**: toolchain resolves but is unwired from the build; the
  declaration index has exactly one placeholder record.

## See also

- `../PRD.md` — the specification this milestone implements.
- `../IMPLEMENTATION_STATUS.md` — full layer-by-layer (7.1-7.10) reconciliation with evidence.
- `../RAIL_A_B_STATUS.md`, `../RAIL_G_MEASUREMENT_DESIGN.md` — deep-dive companion docs.
- `../SAFETY_FINDINGS.md` — the fabricated-capability document and live code-injection pathway
  found in `apps/` this session; read before touching PROJ-755/757/760.
