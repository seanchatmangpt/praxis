# GENESIS — The Seven-Day Program

**Day 1: 2026-07-01.** Principle: build beyond human reading, within human verification. Every day ends with a receipt; no day's claims exceed its mechanisms.

| Day | Program | Exit receipt |
|---|---|---|
| **1 — Foundation & Publication** ✅ **done, sealed** | Land the frontier (all 9 lanes), integrate the proposer + propose verbs, run the walkthrough against the real surface, reconcile the PRD. Publication sweep: license/metadata audit across the constellation, push every repo with a remote, `cargo publish` the lawfully publishable subset (dry-run first; refusals receipted). Genesis manifest: a chained hash over every repo HEAD. | ✅ `docs/genesis/DAY_1_RECEIPT.md` — manifest `f6ec2387…` (re-verified at close), frontier `pass_rate 1.0` (30/30 evaluated of 286 cells), 8/8 publishable crates published, 7 repos pushed (ggen main-push refusal receipted with branch salvage) |
| **2 — Revenue Physics end-to-end** ✅ **sealed** | The full pipe, live: `propose revenue` → ranked proposals → `propose goal` → `plan solve` over a revenue PDDL domain → `law admit` (evidence-gated) → signed receipt → replay conformance 1.0. RevTAC vocabulary v0: revenue operators author missions in ontology, never PDDL. Maximum Reachable Revenue computed, not reported. | ✅ `just revenue-demo` (one command: observation→proposal→admission→receipt, proposal_hash-bound). Receipt: `docs/genesis/DAY_2_RECEIPT.md`; manifest `cb184872…` |
| **3 — Adversarial hardening** ⚠️ **work landed, not sealed** | Fuzz + proptest sweep over every admission boundary (quarantine, config loader, receipt validator, PDDL parser inputs). Mutation-test the chain (affidavit-style operators: drop/reorder/flip). Kill every survivable mutant or receipt why it survives. Bench everything against stated targets. | ⚠️ `crates/praxis-core/tests/{fuzz_boundaries,mutation_chain}.rs` present; **no `MANIFEST_DAY_3.json`** — day did work but sealed no manifest |
| **4 — The Membrane ships** ⚠️ **work landed, not sealed** | MCP+ server productized: full tool surface (law/plan/receipt/propose/frontier/doctor), law-object cache, structured refusals, stdio + docs. A second agent (any MCP client) drives the full Day-2 pipe through the membrane only — proving agents build under law without repo access. | ⚠️ MCP lawobject server + cache present; **no external-agent transcript, no `MANIFEST_DAY_4.json`** |
| **5 — Manufacture at scale** ◑ **receipted + manifest sealed post-hoc; headline target refused as not-met** | ggen lane amplified: N domain ontologies → manufactured PDDL/Rust/docs, byte-deterministic, round-tripped. The DfCm matrix grows axes; every manufactured artifact self-registers a frontier cell. Target: the repo generates more of itself than was hand-written that day. | ◑ Corpus ingest (3 vendored open-ontologies TTLs → refusal map, round-tripped), Kourani WF-net→POWL 2.0 decomposition (**separability = admission predicate**, `L(decompose(N))=L(N)`), indexed grounding (`pddl-index`, byte-identical to naive, **1.96% materialization** on 2.5k candidates). **Manufacture ratio ≈ 0.028 (98 manufactured / 3,564 hand-written LoC) — headline target NOT met, refused in-receipt.** `cargo test --all-features`: 1 sibling `law-signed` parallelism flake (passes isolated/single-threaded), Day-5 surfaces green. Receipt `docs/genesis/DAY_5_RECEIPT.md`; manifest `e563da04…` (`MANIFEST_DAY_5.json`, chains **Day 2** — Day 4 sealed no manifest, not fabricated). Postdates `GENESIS_SEAL.json`; seal not re-sealed. |
| **6 — Mission Physics beyond revenue** ✅ **sealed** (out-of-order, post-Day-7) | Second domain pack: church operations (attendance, welcome completion, care, volunteer time as fluents) — same planner, same admission, different authored values. Proves the substrate is domain-independent by running two institutions on one law. | ✅ church-operations pack + generic `Pack` substrate (`src/mission.rs`); `tests/two_domains.rs` (3 green — incl. `two_institutions_one_substrate`) + `docs/MISSION_PHYSICS.md` now present; `church_proposer_tests` 25 green. Receipt `docs/genesis/DAY_6_RECEIPT.md`; manifest `9f976d7e…` chaining Day 2 (`prev = cb184872…`) — Days 3–5 remain unsealed, recorded as a gap in the manifest's `chain_note`. Sealed **after** the Day-7 `GENESIS_SEAL`; see the SEAL row. |
| **7 — The Genesis release** ◑ **release + Book Three + seal; chain closed over what exists** | Version, changelog, publication of everything lawful. Book Three: *The First Week*, written from the receipts (not memory). The seven-day chain closed: Day 7's receipt commits Day 1's hash. | ◑ Version `26.7.2` + `CHANGELOG.md` + frontier `pass_rate 1.0`; Book Three `docs/fiction/BOOK_THREE_THE_FIRST_WEEK.md`; `docs/genesis/DAY_7_RECEIPT.md` + `GENESIS_SEAL.json`. Push / tag / publish **refused** on a non-quiescent tree (receipted). No `MANIFEST_DAY_7.json`. |
| **SEAL — the week as one object** | Single blake3 hash over the ordered list of sealed daily manifest hashes, so the seal commits Day 1's genesis anchor. Honest length: the program specified 7 daily manifests; 2 were emitted (Days 1–2), both genuine and mutually chained. Days 3–7 did work but sealed no manifest — recorded as gaps, not fabricated. | **`seal_hash = 9c666317edb61ace94d6cc7cc5114a15effd80caa596dbe370191ee2e8dfd34f`** (blake3; `docs/genesis/GENESIS_SEAL.json`). Chain **intact over its 2 sealed links**; both hashes independently recomputed as genuine. **Post-seal addendum:** Day 6 was later sealed for real (`MANIFEST_DAY_6.json`, `9f976d7e…`, chaining Day 2) — a **third** genuine link, emitted *after* this seal was cast. The `seal_hash` above is **not** recomputed here: it remains the true record of what the Day-7 seal actually covered (links 1→2). Folding Day 6 into a fresh week-seal is a follow-up over a quiescent tree; the addition is documented rather than back-dated. |

**Standing rules for all seven days:**
1. Agents implement; the human authors objective functions and law. Nothing self-authorizes.
2. Every refusal is receipted with reason and salvage. Silent gaps are the only forbidden artifact.
3. Publication is explicit: dry-run before publish, refusal-receipt when a crate is unpublishable (path deps, missing licenses).
4. Each day's receipt chains the previous day's. The week is one auditable object.
