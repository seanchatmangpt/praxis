# Chatman Engine Acceptance — Given/When/Then

Generated by ggen from `packs/chatman-engine-pack/ontology.ttl`. Do not edit.

This document mirrors the generated acceptance surface: every acceptance case
is paired with at least one falsification case that mutates a single condition
and must be refused with a named typed `Refusal` variant.

## Acceptance Cases

### Accept_AdmissionTableMatches

- **Given** the fixture `fixtures/admission/admission_table_matches.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_admission.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `admission_table_verified`

### Accept_AgentActsWithinPermits

- **Given** the fixture `fixtures/agents/agent_acts_within_permits.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_agents.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `agent_action_permitted`

### Accept_BoundaryRequestCarriesReceipt

- **Given** the fixture `fixtures/receipts/boundary_request_carries_receipt.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_receipts.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `boundary_receipt_present`

### Accept_DeterministicOperatorWithoutReceipt

- **Given** the fixture `fixtures/hooks/deterministic_operator_without_receipt.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_hooks.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `deterministic_operator_admitted`

### Accept_GraphSnapshotReplaysByteIdentical

- **Given** the fixture `fixtures/replay/graph_snapshot_replays_byte_identical.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_replay.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `snapshot_replay_verified`

### Accept_HookPatternAdmitted

- **Given** the fixture `fixtures/hooks/hook_pattern_admitted.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_hooks.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `hook_pattern_admitted`

### Accept_HotPathRouteForBoundedPattern

- **Given** the fixture `fixtures/routing/hot_path_route_for_bounded_pattern.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_routing.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `hot_path_routed`

### Accept_OcelEventAdmitted

- **Given** the fixture `fixtures/admission/ocel_event_admitted.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_admission.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `ocel_event_admitted`

### Accept_PermittedBreedExecutes

- **Given** the fixture `fixtures/agents/permitted_breed_executes.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_agents.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `breed_execution_permitted`

### Accept_ProfileHashMatches

- **Given** the fixture `fixtures/receipts/profile_hash_matches.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_receipts.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `profile_hash_verified`

### Accept_ProfileSymbolTableAligned

- **Given** the fixture `fixtures/replay/profile_symbol_table_aligned.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_replay.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `symbol_table_aligned`

### Accept_ProjectionHashMatches

- **Given** the fixture `fixtures/receipts/projection_hash_matches.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_receipts.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `projection_hash_verified`

### Accept_SingleProcessReceiptType

- **Given** the fixture `fixtures/static/single_process_receipt_type.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_static.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `process_receipt_type_unique`

### Accept_SnapshotContainsOnlyAtomicTerms

- **Given** the fixture `fixtures/triple8/snapshot_contains_only_atomic_terms.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_triple8.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `snapshot_atomic_terms_only`

### Accept_SupportedDialectRoute

- **Given** the fixture `fixtures/routing/supported_dialect_route.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_routing.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `supported_dialect_routed`

### Accept_TermsResolveInTriple8Universe

- **Given** the fixture `fixtures/triple8/terms_resolve_in_triple8_universe.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_triple8.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `triple8_terms_resolved`

### Accept_Triple8UniverseWithinBounds

- **Given** the fixture `fixtures/triple8/triple8_universe_within_bounds.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_triple8.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `triple8_universe_admitted`

### Accept_UniqueCanonicalTapeType

- **Given** the fixture `fixtures/static/unique_canonical_tape_type.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_static.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `canonical_tape_type_unique`

### Accept_WitnessObservesOnly

- **Given** the fixture `fixtures/agents/witness_observes_only.json` conforming to its schema
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_agents.rs` runs the fixture
- **Then** the engine accepts it and reports behavior tag `witness_observation_recorded`

## Falsification Cases

### Falsify_AdmissionTableMismatch

- **Given** the fixture `fixtures/admission/admission_table_mismatch.json` (mutation: Remove one row from the admission table after its digest was pinned.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_admission.rs` runs the fixture
- **Then** the engine refuses with `Refusal::AdmissionTableMismatch`

### Falsify_AgentOverrideDenied

- **Given** the fixture `fixtures/agents/agent_override_denied.json` (mutation: Have the agent claim an override flag on a decision it holds no permit for.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_agents.rs` runs the fixture
- **Then** the engine refuses with `Refusal::AgentOverrideDenied`

### Falsify_BoundaryRequestMissingReceipt

- **Given** the fixture `fixtures/receipts/boundary_request_missing_receipt.json` (mutation: Strip the receipt field from an otherwise well-formed boundary request.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_receipts.rs` runs the fixture
- **Then** the engine refuses with `Refusal::BoundaryRequestMissingReceipt`

### Falsify_BreedUnpermitted

- **Given** the fixture `fixtures/agents/breed_unpermitted.json` (mutation: Dispatch work to an agent breed absent from the permit list.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_agents.rs` runs the fixture
- **Then** the engine refuses with `Refusal::BreedUnpermitted`

### Falsify_DuplicateCanonicalTapeType

- **Given** the fixture `fixtures/static/duplicate_canonical_tape_type.json` (mutation: Register the CanonicalTape type identifier twice in the type registry.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_static.rs` runs the fixture
- **Then** the engine refuses with `Refusal::DuplicateCanonicalTapeType`

### Falsify_GraphSnapshotMismatch

- **Given** the fixture `fixtures/replay/graph_snapshot_mismatch.json` (mutation: Reorder two quads in the stored snapshot so the replay hash diverges.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_replay.rs` runs the fixture
- **Then** the engine refuses with `Refusal::GraphSnapshotMismatch`

### Falsify_HookPatternNotAdmitted

- **Given** the fixture `fixtures/hooks/hook_pattern_not_admitted.json` (mutation: Register a hook whose trigger pattern uses a predicate outside the admitted set.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_hooks.rs` runs the fixture
- **Then** the engine refuses with `Refusal::HookPatternNotAdmitted`

### Falsify_LeastExpressiveRouteViolation

- **Given** the fixture `fixtures/routing/least_expressive_route_violation.json` (mutation: Request the ColdPath route for a pattern the HotPath already admits.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_routing.rs` runs the fixture
- **Then** the engine refuses with `Refusal::LeastExpressiveRouteViolation`

### Falsify_N3ActuationRefused

- **Given** the fixture `fixtures/routing/n3_actuation_refused.json` (mutation: Mark the N3 request as an actuation attempt, which no profile may actuate.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_routing.rs` runs the fixture
- **Then** the engine refuses with `Refusal::N3ActuationRefused`

### Falsify_N3UnavailableByProfile

- **Given** the fixture `fixtures/routing/n3_unavailable_by_profile.json` (mutation: Route through the N3 dialect while the profile keeps defaultEnabled false.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_routing.rs` runs the fixture
- **Then** the engine refuses with `Refusal::N3UnavailableByProfile`

### Falsify_NondeterministicOperatorRequiresReceipt

- **Given** the fixture `fixtures/hooks/nondeterministic_operator_requires_receipt.json` (mutation: Invoke a nondeterministic operator without attaching the required receipt.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_hooks.rs` runs the fixture
- **Then** the engine refuses with `Refusal::NondeterministicOperatorRequiresReceipt`

### Falsify_OcelEventNotAdmitted

- **Given** the fixture `fixtures/admission/ocel_event_not_admitted.json` (mutation: Submit an OCEL event whose activity name is absent from the admission table.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_admission.rs` runs the fixture
- **Then** the engine refuses with `Refusal::OcelEventNotAdmitted`

### Falsify_ProcessReceiptShadowType

- **Given** the fixture `fixtures/static/process_receipt_shadow_type.json` (mutation: Declare a second struct named ProcessReceipt shadowing the canonical type.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_static.rs` runs the fixture
- **Then** the engine refuses with `Refusal::ProcessReceiptShadowType`

### Falsify_ProfileHashMismatch

- **Given** the fixture `fixtures/receipts/profile_hash_mismatch.json` (mutation: Substitute a different profile document behind an unchanged profile hash claim.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_receipts.rs` runs the fixture
- **Then** the engine refuses with `Refusal::ProfileHashMismatch`

### Falsify_ProfileSymbolTableMismatch

- **Given** the fixture `fixtures/replay/profile_symbol_table_mismatch.json` (mutation: Swap two symbol ids in the profile symbol table against the pinned interning.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_replay.rs` runs the fixture
- **Then** the engine refuses with `Refusal::ProfileSymbolTableMismatch`

### Falsify_ProjectionHashMismatch

- **Given** the fixture `fixtures/receipts/projection_hash_mismatch.json` (mutation: Flip one byte of the projection payload after its hash was recorded.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_receipts.rs` runs the fixture
- **Then** the engine refuses with `Refusal::ProjectionHashMismatch`

### Falsify_RouteDecisionMismatch

- **Given** the fixture `fixtures/routing/route_decision_mismatch.json` (mutation: Alter the recorded route decision literal so it disagrees with the recomputed route.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_routing.rs` runs the fixture
- **Then** the engine refuses with `Refusal::RouteDecisionMismatch`

### Falsify_TermNotInTriple8Universe

- **Given** the fixture `fixtures/triple8/term_not_in_triple8_universe.json` (mutation: Reference one term IRI that was never interned into the Triple8 universe.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_triple8.rs` runs the fixture
- **Then** the engine refuses with `Refusal::TermNotInTriple8Universe`

### Falsify_Triple8UniverseOverflow

- **Given** the fixture `fixtures/triple8/triple8_universe_overflow.json` (mutation: Add a ninth distinct term so the universe exceeds the Triple8 bound of eight.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_triple8.rs` runs the fixture
- **Then** the engine refuses with `Refusal::Triple8UniverseOverflow`

### Falsify_Triple8UniverseOverflowByOne

- **Given** the fixture `fixtures/triple8/triple8_universe_overflow_by_one.json` (mutation: Duplicate one admitted term under a fresh IRI so the universe count becomes nine.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_triple8.rs` runs the fixture
- **Then** the engine refuses with `Refusal::Triple8UniverseOverflow`

### Falsify_TripleTermInSnapshot

- **Given** the fixture `fixtures/triple8/triple_term_in_snapshot.json` (mutation: Embed one RDF-star triple term inside the snapshot term list.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_triple8.rs` runs the fixture
- **Then** the engine refuses with `Refusal::TripleTermInSnapshot`

### Falsify_UnsupportedDialect

- **Given** the fixture `fixtures/routing/unsupported_dialect.json` (mutation: Name a dialect outside the closed dialect vocabulary.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_routing.rs` runs the fixture
- **Then** the engine refuses with `Refusal::UnsupportedDialect`

### Falsify_WarmPathRequired

- **Given** the fixture `fixtures/routing/warm_path_required.json` (mutation: Raise the pattern constraint count above the HotPath maxConstraints of eight.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_routing.rs` runs the fixture
- **Then** the engine refuses with `Refusal::WarmPathRequired`

### Falsify_WitnessNotAuthority

- **Given** the fixture `fixtures/agents/witness_not_authority.json` (mutation: Have a witness role attempt to author an authoritative decision triple.)
- **When** the harness in `crates/praxis-graphlaw/tests/chatman_acceptance_agents.rs` runs the fixture
- **Then** the engine refuses with `Refusal::WitnessNotAuthority`

## See Also

- `packs/chatman-engine-pack/ontology.ttl` — source of truth for this document
- `docs/standing/REALITY_INDEX.md` — standing verification policy
