# GENERATED BY ggen — do not edit. Source: crates/praxis-graphlaw/ontologies/core/otel-bridge.ttl
groups:
  - id: event.praxis.activity_executed
    type: event
    name: praxis.activity_executed
    stability: development
    brief: Records an observed process activity occurrence
    attributes:
      - id: process.workflow.id
        type: string
        stability: development
        requirement_level: required
        brief: Identifier of the workflow (process instance) this activity occurrence belongs to.
        examples: ["process_workflow_id_sample"]
      - id: process.object.id
        type: string
        stability: development
        requirement_level: required
        brief: Unique identifier of the business object the activity touched.
        examples: ["process_object_id_sample"]
      - id: process.object.type
        type: string
        stability: development
        requirement_level: required
        brief: Named category of the business object (OCEL 2.0 object type).
        examples: ["process_object_type_sample"]
      - id: process.activity.iri
        type: string
        stability: development
        requirement_level: required
        brief: IRI of the executed activity.
        examples: ["process_activity_iri_sample"]
      - id: process.outcome
        type: string
        stability: development
        requirement_level: required
        brief: Typed completion outcome of the activity occurrence.
        examples: ["process_outcome_sample"]
