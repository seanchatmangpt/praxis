name: Release

on:
  push:
    tags:
      - "[0-9][0-9].[0-9]+.[0-9]+"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            use_cross: false
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            use_cross: true
          - target: aarch64-apple-darwin
            os: macos-latest
            use_cross: false
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            use_cross: false

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - uses: dtolnay/rust-toolchain@b3b07ba8b418998c39fb20f53e8b695cdcc8de1b  # stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@f13886b937689c021905a6b90929199931d60db1  # v2.8.1
        with:
          key: ${{ matrix.target }}

      - name: Install cross (aarch64-unknown-linux-gnu)
        if: matrix.use_cross
        run: cargo install cross --locked

      - name: Build release binary (cross)
        if: matrix.use_cross
        run: cross build --release --target ${{ matrix.target }}

      - name: Build release binary (cargo)
        if: "!matrix.use_cross"
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Stage artifact (Unix)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          set -euo pipefail
          BINARY="target/${{ matrix.target }}/release/project-name"
          ARTIFACT="project-name-${{ github.ref_name }}-${{ matrix.target }}"
          cp "$BINARY" "$ARTIFACT"
          echo "ARTIFACT=$ARTIFACT" >> "$GITHUB_ENV"

      - name: Stage artifact (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $BINARY = "target/${{ matrix.target }}/release/project-name.exe"
          $ARTIFACT = "project-name-${{ github.ref_name }}-${{ matrix.target }}.exe"
          Copy-Item $BINARY $ARTIFACT
          "ARTIFACT=$ARTIFACT" | Out-File -FilePath $env:GITHUB_ENV -Append

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4.6.2
        with:
          name: project-name-${{ github.ref_name }}-${{ matrix.target }}
          path: ${{ env.ARTIFACT }}
          if-no-files-found: error
          retention-days: 1

  release:
    name: GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@fc02353415da80201a0da48ab47022efd7725d11  # v4.3.0
        with:
          path: artifacts
          merge-multiple: true

      - uses: softprops/action-gh-release@62c96d0c4e8a889135c1f3a25910db8dbe0e85f7  # v2.3.4
        with:
          generate_release_notes: true
          files: artifacts/*

# Uncomment to publish to crates.io on release.
# Requires CARGO_REGISTRY_TOKEN secret set in repository settings.
# For multi-crate workspaces, publish in dependency order with --locked.
#
#  publish:
#    name: crates.io
#    needs: release
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
#
#      - uses: dtolnay/rust-toolchain@b3b07ba8b418998c39fb20f53e8b695cdcc8de1b  # stable
#        with:
#          toolchain: stable
#
#      - name: Publish crate
#        run: cargo publish --locked
#        env:
#          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
